services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nemotours_app_local
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - nemotours-local-network
    depends_on:
      - db
      - redis
    env_file:
      - .env
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
      - DB_HOST=db
      - DB_PORT=3306
      - REDIS_HOST=redis
      - REDIS_PORT=6379

  nginx:
    image: nginx:alpine
    container_name: nemotours_nginx_local
    restart: unless-stopped
    ports:
      - "8017:80"   # Port مختلف للـ local
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/default-local.conf:/etc/nginx/conf.d/default.conf
    networks:
      - nemotours-local-network
    depends_on:
      - app

  db:
    image: mysql:8.0
    container_name: nemotours_db_local
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-nemotours_db_local}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-${DB_PASSWORD:-nemotoursroot}}
      MYSQL_PASSWORD: ${DB_PASSWORD:-nemotours123}
      MYSQL_USER: ${DB_USERNAME:-nemotours}
    volumes:
      - nemotours_dbdata_local:/var/lib/mysql
    ports:
      - "3308:3306"   # Port مختلف للـ local
    networks:
      - nemotours-local-network
    command: --default-authentication-plugin=mysql_native_password

  redis:
    image: redis:7-alpine
    container_name: nemotours_redis_local
    restart: unless-stopped
    ports:
      - "6381:6379"  # Port مختلف للـ local
    volumes:
      - nemotours_redisdata_local:/data
    networks:
      - nemotours-local-network

networks:
  nemotours-local-network:
    driver: bridge

volumes:
  nemotours_dbdata_local:
    driver: local
  nemotours_redisdata_local:
    driver: local
