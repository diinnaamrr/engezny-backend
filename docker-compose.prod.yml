services:
  app:
    image: ghcr.io/osama21245/wayak-back:${IMAGE_TAG:-latest}
    container_name: nemotours_app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
      - ./storage:/var/www/html/storage
      - ./bootstrap/cache:/var/www/html/bootstrap/cache
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - nemotours-network
    depends_on:
      - db
      - redis
    env_file:
      - .env
    environment:
      - APP_ENV=${APP_ENV:-production}
      - APP_DEBUG=${APP_DEBUG:-false}
      - DB_HOST=db
      - DB_PORT=3306
      - REDIS_HOST=redis
      - REDIS_PORT=6379

  nginx:
    image: nginx:alpine
    container_name: nemotours_nginx
    restart: unless-stopped
    ports:
      - "8015:80"  # بورت جديد مخصص للـ nemotours
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./docker/nginx/ssl:/etc/nginx/ssl:ro
    networks:
      - nemotours-network
    depends_on:
      - app

  db:
    image: mysql:8.0
    container_name: nemotours_db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-nemotours_db}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-${DB_PASSWORD}}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USERNAME:-nemotours}
    volumes:
      - nemotours_dbdata:/var/lib/mysql
      - ./backups:/backups
    networks:
      - nemotours-network
    command: --default-authentication-plugin=mysql_native_password

  redis:
    image: redis:7-alpine
    container_name: nemotours_redis
    restart: unless-stopped
    volumes:
      - nemotours_redisdata:/data
    networks:
      - nemotours-network
    command: redis-server --appendonly yes

networks:
  nemotours-network:
    driver: bridge

volumes:
  nemotours_dbdata:
    driver: local
  nemotours_redisdata:
    driver: local
